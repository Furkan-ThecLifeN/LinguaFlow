import { Song, Word, UserProgress } from './types';

// Empty initial state for a fresh application
export const MOCK_WORDS: Word[] = [];

export const MOCK_SONGS: Song[] = [];

export const MOCK_PROGRESS: UserProgress[] = [];
